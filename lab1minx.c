
#include <stdio.h>

int main() // ข้อที่ 1
{
    int a=10;
    int b=15;
    if (b>a)
    {
        printf("%d greater than %d",b,a);
    }
    if(a>b)
    {
        printf("%d greater than %d",a,b);
    }
    return 0;
}